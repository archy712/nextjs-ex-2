# 노션 기반 견적서 관리 시스템 MVP PRD

## 🎯 핵심 정보

**목적**: Notion에 입력된 견적서 데이터를 클라이언트가 로그인 없이 웹에서 바로 조회하고 PDF로 저장할 수 있게 한다. 아울러 발행자(운영자)가 Notion을 열지 않고도 웹 관리자 영역에서 전체 견적서 목록을 확인하고 클라이언트 조회 링크를 복사할 수 있게 한다.
**사용자**: Notion으로 견적서를 작성하는 1인 사업자/소규모 팀, 링크를 받아 견적서를 확인하는 클라이언트, 그리고 관리자 영역을 사용하는 운영자(발행자 본인).

## 🚶 사용자 여정

### 견적서 발행자 — v1 여정 (Notion에서 직접 작업)

```
1. Notion "견적서" 데이터베이스에 새 페이지 생성
   ↓ 클라이언트명 / 유효기간 / 항목(Items 데이터베이스와 관계로 연결) 입력

2. Notion이 페이지 ID 기반 조회 URL을 수식(Formula) 속성으로 자동 계산
   ↓ 해당 셀 값 복사

3. 카카오톡/이메일 등으로 클라이언트에게 링크 수동 전달
   ↓ (서비스가 자동 발송하지 않음 — 앱 범위 밖)

4. 완료 — 이후 Notion에서 내용을 수정하면, 클라이언트가 다시 열람할 때 항상 최신 데이터로 표시됨
```

### 견적서 발행자 — v2 여정 (관리자 영역 사용)

```
1. /admin 접속 → 세션 없음 → /admin/login으로 리다이렉트
   ↓ 비밀번호 입력 후 로그인

2. 관리자 목록 화면에서 전체 견적서(번호/클라이언트명/유효기간/합계 금액) 확인
   ↓ 원하는 견적서 행의 "링크 복사" 버튼 클릭

3. 클립보드에 담긴 클라이언트 조회 링크를 카카오톡/이메일 등으로 전달
   ↓ (v1과 동일하게 서비스가 자동 발송하지 않음)

4. 완료 — 필요 시 로그아웃. 위 "Notion에서 포뮬러 셀 복사" 경로는 계속 사용 가능한 대안으로 유지됨
```

### 클라이언트 (견적서 수신자, 실제 앱 사용자)

```
1. 전달받은 견적서 링크 클릭
   ↓ 견적서 조회 페이지 접속 시도 (ID 형식 검증 + Notion 데이터 조회)

   [정상 조회]        → 견적서 조회 페이지 → 내용 확인
   [ID 형식 오류/미존재] → 오류 페이지 (404)
   [Notion API 장애]   → 오류 페이지 (503)
   ↓

2. (정상 경로) 견적서 조회 페이지에서 클라이언트명·항목·금액 등 확인
   ↓ PDF 다운로드 버튼 클릭 (브라우저 인쇄 기능 실행)

3. 인쇄 대화상자에서 "PDF로 저장" 선택
   ↓

4. 완료 → 같은 페이지 유지 (필요 시 재확인/재저장 가능)
```

## ⚡ 기능 명세

### 1. MVP 핵심 기능

| ID | 기능명 | 설명 | MVP 필수 이유 | 관련 페이지 |
|----|--------|------|-------------|------------|
| **F001** | Notion 데이터 조회 | Notion API로 견적서 1건과 연결된 항목 목록을 조회 | 이 서비스의 유일한 데이터 소스 | 견적서 조회 페이지 |
| **F002** | 견적서 내용 표시 | 클라이언트명, 유효기간, 항목별 수량/단가/금액, 합계 금액을 화면에 표시 | 클라이언트가 견적서를 확인하는 핵심 기능 | 견적서 조회 페이지 |
| **F003** | PDF 다운로드(인쇄) | `window.print()`와 인쇄 전용 스타일로 화면을 그대로 PDF로 저장 | 클라이언트가 견적서를 보관/제출하기 위한 필수 기능 | 견적서 조회 페이지 |

### 2. MVP 필수 지원 기능

| ID | 기능명 | 설명 | MVP 필수 이유 | 관련 페이지 |
|----|--------|------|-------------|------------|
| **F010** | 견적서 ID 형식 검증 | URL의 견적서 ID가 유효한 Notion 페이지 ID 형식인지 확인 | 잘못된 요청을 Notion API 호출 전에 조기 차단 | 견적서 조회 페이지, 오류 페이지 |
| **F011** | 존재하지 않는 견적서 안내 (404) | ID 형식은 맞지만 Notion에 해당 견적서가 없거나 접근 범위 밖일 때 안내 | 잘못된/만료된 링크에 대한 명확한 피드백 | 오류 페이지 |
| **F012** | 서비스 장애 안내 (503) | Notion API 오류·응답 지연 시 별도 안내 표시 | 데이터 소스 장애와 "존재하지 않음"을 구분해 혼동 방지 | 오류 페이지 |
| **F013** | 반응형 레이아웃 | 모바일/태블릿/데스크톱 화면 대응 | 클라이언트가 어떤 기기로 링크를 열어도 확인 가능해야 함 | 견적서 조회 페이지 |

### 3. v2 관리자 기능

| ID | 기능명 | 설명 | 필수 이유 | 관련 페이지 |
|----|--------|------|-------------|------------|
| **F020** | 관리자 견적서 목록 조회 | Notion Invoices 데이터베이스의 모든 견적서를 조회해 견적서 번호·클라이언트명·유효기간·합계 금액을 목록으로 표시 | 발행자가 Notion을 열지 않고도 전체 현황을 확인해야 함 | 관리자 견적서 목록 페이지 |
| **F021** | 클라이언트 조회 링크 복사 | 목록의 각 행에서 해당 견적서의 클라이언트 조회 링크(`/invoice/[id]`)를 클립보드로 복사 | 링크를 얻으려 Notion 포뮬러 셀을 매번 복사하던 수고를 없앰 | 관리자 견적서 목록 페이지 |
| **F022** | 관리자 비밀번호 게이트 | 서버 전용 환경변수의 단일 비밀번호로 `/admin` 영역 보호(로그인/로그아웃/세션 쿠키) | 관리자 영역은 운영자 본인만 접근해야 함 | 관리자 로그인 페이지 |
| **F023** | 관리자 오류·빈 상태 안내 | Notion 장애(503)·견적서 0건·로그인 실패를 구분해 안내 | v1과 동일하게 장애·빈 상태·인증 실패를 혼동 없이 전달해야 함 | 관리자 견적서 목록 페이지, 관리자 로그인 페이지 |
| **F024** | 관리자 영역 반응형 레이아웃 | 모바일/태블릿/데스크톱 대응 | 운영자가 휴대폰에서도 목록을 확인하고 링크를 복사할 수 있어야 함 | 관리자 견적서 목록 페이지, 관리자 로그인 페이지 |

### 4. MVP 이후 기능 (제외)

- 통계·차트는 계속 제외, 목록 조회·단일 비밀번호 게이트는 v2(F020~F024)에서 도입 (정식 회원가입·다중 사용자 세션 관리는 여전히 범위 밖)
- 견적서 상태 관리 워크플로우 (승인/거절/대기 추적)
- 이메일/카카오톡 자동 발송
- 견적서 링크 만료·개별 견적서 비밀번호
- 견적서 생성·수정·삭제(쓰기 작업 — Notion은 계속 읽기 전용), 목록 검색·필터·정렬 UI
- 부가세·할인 등 세부 금액 계산 (합계 금액만 다룸)
- 다국어 지원, 견적서 템플릿 커스터마이징, 버전 관리

## 📱 메뉴 구조

```
📱 견적서 시스템
├── 📄 견적서 조회 페이지 (완전 무인증, 고유 링크로만 접근 — 기본 진입점)
│   └── 기능: F001, F002, F003, F010, F013
├── ⚠️ 오류 페이지 (사용자가 직접 이동하지 않음 — 조건 충족 시 자동 표시)
│   └── 기능: F010, F011, F012
├── 🔒 /admin — 관리자 견적서 목록 페이지 (비밀번호 로그인 필요)
│   └── 기능: F020, F021, F023, F024
└── 🔑 /admin/login — 관리자 로그인 페이지 (비밀번호 로그인 필요)
    └── 기능: F022, F023, F024
```

**참고**: 견적서 조회 페이지·오류 페이지는 v1과 동일하게 완전 무인증이며 변경이 없다. 관리자 영역(`/admin`, `/admin/login`)만 비밀번호 로그인이 필요하다. 정식 회원가입, 발행자 전용 메뉴 이외의 공통 메뉴(메시지/알림/설정) 등은 이 서비스에 존재하지 않으므로 메뉴 구조에 포함하지 않음.

---

## 📄 페이지별 상세 기능

### 견적서 조회 페이지

> **구현 기능:** `F001`, `F002`, `F003`, `F010`, `F013` | **접근 방식:** 고유 URL (인증 불필요)

| 항목 | 내용 |
|------|------|
| **역할** | 클라이언트가 고유 링크로 견적서 내용을 확인하고 PDF로 저장하는 유일한 조회 화면 |
| **진입 경로** | 발행자가 전달한 견적서 링크 클릭 (예: 견적서 조회 페이지 + 고유 ID) |
| **사용자 행동** | • 클라이언트명·유효기간·항목별 금액·합계 금액 확인<br>• PDF 다운로드 버튼 클릭<br>• 인쇄 대화상자에서 PDF로 저장 |
| **주요 기능** | • ID 형식 검증 후 Notion에서 견적서+항목 데이터 조회<br>• 발행일, 유효기간, 항목별 수량/단가/금액, 합계 금액 렌더링<br>• 인쇄 전용 스타일(`@media print`)로 다운로드 버튼 등 불필요한 UI 숨김<br>• **PDF 다운로드** 버튼 (클릭 시 `window.print()` 실행) |
| **다음 이동** | ID 형식 오류/존재하지 않는 견적서 → 오류 페이지(404), Notion 장애 → 오류 페이지(503), PDF 저장 완료 → 같은 페이지 유지 |

---

### 오류 페이지 (404 / 503)

> **구현 기능:** `F010`, `F011`, `F012` | **접근 방식:** 자동 표시 (사용자가 직접 이동하지 않음)

| 항목 | 내용 |
|------|------|
| **역할** | 잘못된 견적서 링크 접근과 Notion 장애 상황을 구분해 안내하는 오류 전용 화면 |
| **진입 경로** | 견적서 조회 페이지 진입 시 ID 검증 실패, 견적서 미존재, 또는 Notion API 오류가 감지되면 자동 표시 |
| **사용자 행동** | • 안내 메시지 확인<br>• 발행자에게 올바른 링크를 다시 요청 |
| **주요 기능** | • 404: "견적서를 찾을 수 없습니다" (ID 형식 오류·미존재·접근 범위 밖 모두 포함)<br>• 503: "일시적으로 서비스를 이용할 수 없습니다. 잠시 후 다시 시도해 주세요" (Notion API 오류/지연) |
| **다음 이동** | 별도 이동 없음 — 사용자가 올바른 링크를 다시 받아 재접속해야 함 |

---

### 관리자 견적서 목록 페이지

> **구현 기능:** `F020`, `F021`, `F023`, `F024` | **접근 방식:** `/admin` (비밀번호 로그인 필요)

| 항목 | 내용 |
|------|------|
| **역할** | 운영자가 전체 견적서 현황을 확인하고 클라이언트에게 보낼 조회 링크를 복사하는 관리자 전용 목록 화면 |
| **진입 경로** | `/admin` 접속 → 세션 있으면 바로 진입, 없으면 관리자 로그인 페이지로 리다이렉트 |
| **사용자 행동** | • 견적서 번호·클라이언트명·유효기간·합계 금액 확인<br>• 원하는 행의 링크 복사 버튼 클릭<br>• 로그아웃 |
| **주요 기능** | • Notion Invoices 데이터베이스 전체 견적서 목록 조회<br>• 클라이언트 조회 링크(`/invoice/[id]`) 클립보드 복사<br>• Notion 장애(503)·견적서 0건 빈 상태를 구분해 안내<br>• 모바일/태블릿/데스크톱 반응형 레이아웃 |
| **다음 이동** | 로그아웃 → 관리자 로그인 페이지, 견적서 번호 클릭 → 견적서 조회 페이지(새 창) |

---

### 관리자 로그인 페이지

> **구현 기능:** `F022`, `F023`, `F024` | **접근 방식:** `/admin/login` (비인증 상태에서만 의미 있는 진입점)

| 항목 | 내용 |
|------|------|
| **역할** | 서버 전용 환경변수의 단일 비밀번호로 관리자 세션을 발급하는 로그인 화면 |
| **진입 경로** | 세션 없이 `/admin` 접속 시 자동 리다이렉트, 또는 로그아웃 후 재방문 |
| **사용자 행동** | • 비밀번호 입력 후 제출<br>• 실패 시 안내 메시지 확인 후 재시도 |
| **주요 기능** | • 비밀번호 검증(타이밍 안전 비교) 후 서명된 HttpOnly 쿠키 세션 발급<br>• 실패 시 "비밀번호가 올바르지 않습니다"만 안내(내부 정보 미노출)<br>• 모바일/태블릿/데스크톱 반응형 레이아웃 |
| **다음 이동** | 로그인 성공 → 관리자 견적서 목록 페이지(`/admin`) |

---

## 🗄️ 데이터 모델

> Notion을 유일한 데이터 저장소로 사용하며 별도 DB에 복제하지 않음(읽기 전용). 라인아이템은 페이지 내부 표가 아닌 **하위 관계형 데이터베이스**로 표현해, 합계 금액을 Notion이 자동 계산(Rollup)하도록 한다.

### Invoices (견적서)

| 필드 | 설명 | 타입/관계 |
|------|------|----------|
| id | Notion 페이지 고유 ID (조회 URL에 사용) | String (UUID) |
| invoice_number | 견적서 번호 (제목) | Title |
| client_name | 클라이언트명 | Rich Text |
| valid_until | 유효기간 | Date |
| items | 견적 항목 목록 | Relation → Items |
| total_amount | 합계 금액 (항목 금액의 합) | Rollup (Sum) |

### Items (견적 항목)

| 필드 | 설명 | 타입/관계 |
|------|------|----------|
| id | 항목 고유 ID | String |
| description | 항목 설명 (제목) | Title |
| quantity | 수량 | Number |
| unit_price | 단가 | Number |
| amount | 금액 (수량 × 단가) | Formula |
| invoice | 연결된 견적서 | Relation → Invoices |

## 🛠️ 기술 스택 (최신 버전)

### 🎨 프론트엔드 프레임워크

- **Next.js 16.2** (App Router, Turbopack) - 이 저장소에 이미 설치된 프레임워크
- **React 19.2** - UI 라이브러리
- **TypeScript 5.9** - 타입 안전성 보장

### 🎨 스타일링 & UI

- **TailwindCSS v4** (설정 파일 없는 CSS-first 엔진, `app/globals.css`의 `@theme inline`) - 유틸리티 CSS
- **shadcn/ui** (`radix-ui` 패키지 기반, `style: "radix-nova"`) - `components/ui/*`의 `table`, `card`, `badge`, `skeleton` 등 재사용
- **Lucide React** - 아이콘 라이브러리

### 📝 검증

- **Zod 4.4** - 견적서 ID 라우트 파라미터 형식 검증, Notion 응답 매핑 검증, 관리자 환경변수·로그인 입력 검증에 사용
- `react-hook-form`/`@hookform/resolvers`는 패키지로 남아있으나 필드 1개짜리 로그인 폼에는 과설계이므로 사용하지 않음

### 🔐 인증 방식

- 서버 전용 환경변수 단일 비밀번호 + 서명된 HttpOnly 쿠키 세션(정식 인증 라이브러리·Supabase Auth 미도입) — 관리자 영역(`/admin`)에만 적용되며, 견적서 조회 페이지는 v1과 동일하게 완전 무인증

### 🗄️ 데이터 소스

- **Notion API** (`@notionhq/client` 최신 버전, 신규 설치 필요) - 견적서/항목 데이터 조회 전용 (쓰기 없음)

### 📄 PDF 생성

- **브라우저 인쇄 기능** (`window.print()` + `@media print` CSS) - 별도 서버사이드 PDF 라이브러리 없이, 견적서 조회 페이지 화면을 그대로 인쇄 대화상자를 통해 PDF로 저장

### 🚀 배포 & 호스팅

- **Vercel** - Next.js 최적화 배포 플랫폼

### 📦 패키지 관리

- **npm** - 의존성 관리

---

## ✅ 정합성 검증 결과

- 기능 명세(F001~F013, F020~F024)의 모든 항목이 메뉴 구조와 페이지별 상세 기능(견적서 조회 페이지, 오류 페이지, 관리자 견적서 목록 페이지, 관리자 로그인 페이지)에 매핑됨
- 메뉴 구조의 모든 항목이 페이지별 상세 기능에 존재하며, 참조하는 기능 ID가 기능 명세에 모두 정의됨
- 페이지별 상세 기능에 기재된 구현 기능 ID가 기능 명세와 완전히 일치함 (누락/고아 항목 없음)
- "완전 무인증"은 견적서 조회 페이지 한정 서술로 정정되었으며, 관리자 영역(F022)의 비밀번호 게이트와 모순되지 않음
- "MVP 이후 기능(제외)" 목록과 v2 관리자 기능 표(F020~F024)가 서로 모순되지 않음(통계/차트만 계속 제외, 목록·게이트는 도입)
